--- a/drivers/net/ethernet/airoha/arht_dp_api.c
+++ b/drivers/net/ethernet/airoha/arht_dp_api.c
@@ -172,8 +172,6 @@ QDMA_Private_T *gpQdmaPriv = NULL ;
 extern struct airoha_eth *glb_eth;
 extern int (*airoha_ppe_foe_commit_entry_ptr)(struct airoha_ppe *ppe,struct airoha_foe_entry *e,u32 hash,bool rx_wlan);
 
-extern u32 airoha_ppe_foe_get_entry_hash(struct airoha_foe_entry *hwe);
-
 extern bool airoha_ppe_foe_compare_entry(struct airoha_flow_table_entry *e,
 					 struct airoha_foe_entry *hwe);
 extern void airoha_ppe_delete_entry(struct airoha_ppe *ppe, struct airoha_foe_entry *hwe, uint entry_idx);
@@ -1349,8 +1347,6 @@ before 7523 time to meter window size is
     (airoha_qdma_rr(qdma, QDMA_CSR_EGRESS_RATELIMIT_CFG & EGRESS_RATELIMIT_BUCKETSCALE_MASK) >> EGRESS_RATELIMIT_BUCKETSCALE_SHIFT)
 	
 
-#define MAX(a, b) ((a>b)?(a):(b))
-
 #define SWITCH_BUFFER_THRESHOLD 40 /* unit is 40kByte */
 
 /*
@@ -9155,7 +9151,11 @@ int airoha_check_flood_packet(struct sk_
 	if (netdev_uses_dsa(tmp_dev))
 	{
 		port_idx = ffs(tag)-1;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 12, 0)
+		tmp_dev = dsa_conduit_find_user(tmp_dev, 0, port_idx);
+#else
 		tmp_dev = dsa_master_find_slave(tmp_dev, 0, port_idx);
+#endif
 		if(!tmp_dev)
 			return 0;
 
@@ -9522,14 +9522,14 @@ void airoha_ppe_foe_flow_update_eth_offl
 		airoha_ppe_pon_sfu_bind(ppe, hwe, skb, pinfo, fport);
 		goto unlock;
 	}
-	
-	index = airoha_ppe_foe_get_entry_hash(hwe);
+
+	index = airoha_ppe_foe_get_entry_hash(ppe, hwe);
 	hlist_for_each_entry_safe(e, n, &ppe->foe_flow[index], list) {
 		if (airoha_ppe_foe_compare_entry(e, hwe)) {
 			if(e->tx_modified){
 				goto unlock;
 			}
-			type = FIELD_GET(AIROHA_FOE_IB1_BIND_PACKET_TYPE, e->data.ib1);			
+			type = FIELD_GET(AIROHA_FOE_IB1_BIND_PACKET_TYPE, e->data.ib1);
 			dscp = get_dscp_from_skb(skb, type);
 			
 			switch (type) {
@@ -9605,8 +9605,8 @@ void airoha_ppe_foe_flow_update_pon_offl
 		airoha_ppe_pon_sfu_bind(ppe, hwe, skb, pinfo, 2);
 		goto unlock;
 	}
-		
-	index = airoha_ppe_foe_get_entry_hash(hwe);
+
+	index = airoha_ppe_foe_get_entry_hash(ppe, hwe);
 	hlist_for_each_entry_safe(e, n, &ppe->foe_flow[index], list) {
 		if (airoha_ppe_foe_compare_entry(e, hwe)) {
 			if(e->tx_modified){
@@ -9872,34 +9872,33 @@ ipv6_header:
 }
 
 int airoha_eth_transmit_packet(struct sk_buff *skb, u32 txmsg0, u32 txmsg1, struct port_info *pinfo){
-	struct net_device *dev = glb_eth->ports[0]->dev;
-	struct airoha_gdm_port *port = netdev_priv(dev);
-	struct airoha_qdma *qdma = port->qdma;
+	struct airoha_gdm_port *port = glb_eth->ports[0];
+	struct net_device *dev = port->devs[0]->dev;
+	struct airoha_qdma *qdma = port->devs[0]->qdma;
 	u32 nr_frags, len;
+	struct airoha_queue_entry *e;
 	struct netdev_queue *txq;
 	struct airoha_queue *q;
-	
+	LIST_HEAD(tx_list);
 	void *data;
 	int i, qid;
 	u16 index;
-	//u8 fport;
+
 	skb->dev = dev;
-	
+
 	qid = skb_get_queue_mapping(skb) % ARRAY_SIZE(qdma->q_tx);
-	//tag = DP_SPEED_UP;//use in sptag for pingpong stream
 
 	txmsg0 = FIELD_PREP(QDMA_ETH_TXMSG_TCO_MASK, 1) |
 		   FIELD_PREP(QDMA_ETH_TXMSG_CHAN_MASK,
 			  qid / AIROHA_NUM_QOS_QUEUES) |
 		   FIELD_PREP(QDMA_ETH_TXMSG_QUEUE_MASK,
 			  qid % AIROHA_NUM_QOS_QUEUES) ;
-	
+
 	if (skb->ip_summed == CHECKSUM_PARTIAL)
 		txmsg0 |= FIELD_PREP(QDMA_ETH_TXMSG_TCO_MASK, 1) |
 			FIELD_PREP(QDMA_ETH_TXMSG_UCO_MASK, 1) |
 			FIELD_PREP(QDMA_ETH_TXMSG_ICO_MASK, 1);
 
-	/* TSO: fill MSS info in tcp checksum field */
 	if (skb_is_gso(skb)) {
 		if (skb_cow_head(skb, 0))
 			goto error;
@@ -9913,11 +9912,8 @@ int airoha_eth_transmit_packet(struct sk
 					FIELD_PREP(QDMA_ETH_TXMSG_UCO_MASK, 1) |
 					FIELD_PREP(QDMA_ETH_TXMSG_ICO_MASK, 1);
 		}
-	} 
-	
-	//fport = FE_PSE_PORT_PPE1;
-	
-//	msg1 =0x7f4007ff;
+	}
+
 	q = &qdma->q_tx[qid];
 	if (WARN_ON_ONCE(!q->ndesc))
 		goto error;
@@ -9927,8 +9923,7 @@ int airoha_eth_transmit_packet(struct sk
 	txq = netdev_get_tx_queue(dev, qid);
 	nr_frags = 1 + skb_shinfo(skb)->nr_frags;
 
-	if (q->queued + nr_frags > q->ndesc) {
-		/* not enough space in the queue */
+	if (q->queued + nr_frags >= q->ndesc) {
 		netif_tx_stop_queue(txq);
 		spin_unlock_irq(&q->lock);
 		return NETDEV_TX_BUSY;
@@ -9936,43 +9931,44 @@ int airoha_eth_transmit_packet(struct sk
 
 	len = skb_headlen(skb);
 	data = skb->data;
-	index = q->head;
+
+	e = list_first_entry(&q->tx_list, struct airoha_queue_entry,
+			     list);
+	index = e - q->entry;
 
 	for (i = 0; i < nr_frags; i++) {
 		struct airoha_qdma_desc *desc = &q->desc[index];
-		struct airoha_queue_entry *e = &q->entry[index];
 		skb_frag_t *frag = &skb_shinfo(skb)->frags[i];
 		dma_addr_t addr;
 		u32 val;
+
 		addr = dma_map_single(dev->dev.parent, data, len,
-					  DMA_TO_DEVICE);
+				      DMA_TO_DEVICE);
 		if (unlikely(dma_mapping_error(dev->dev.parent, addr)))
 			goto error_unmap;
 
-		index = (index + 1) % q->ndesc;
+		list_move_tail(&e->list, &tx_list);
+		e->skb = i ? NULL : skb;
+		e->dma_addr = addr;
+		e->dma_len = len;
+
+		e = list_first_entry(&q->tx_list, struct airoha_queue_entry,
+				     list);
+		index = e - q->entry;
 
 		val = FIELD_PREP(QDMA_DESC_LEN_MASK, len);
-		if (i < nr_frags - 1){
+		if (i < nr_frags - 1)
 			val |= FIELD_PREP(QDMA_DESC_MORE_MASK, 1);
-		}
 		WRITE_ONCE(desc->ctrl, cpu_to_le32(val));
 		WRITE_ONCE(desc->addr, cpu_to_le32(addr));
-
 		val = FIELD_PREP(QDMA_DESC_NEXT_ID_MASK, index);
 		WRITE_ONCE(desc->data, cpu_to_le32(val));
 		WRITE_ONCE(desc->msg0, cpu_to_le32(txmsg0));
 		WRITE_ONCE(desc->msg1, cpu_to_le32(txmsg1));
 
-		e->skb = i ? NULL : skb;
-		e->dma_addr = addr;
-		e->dma_len = len;
-
 		data = skb_frag_address(frag);
 		len = skb_frag_size(frag);
 	}
-	
-
-	q->head = index;
 	q->queued += i;
 
 	skb_tx_timestamp(skb);
@@ -9981,7 +9977,7 @@ int airoha_eth_transmit_packet(struct sk
 	if (netif_xmit_stopped(txq) || !netdev_xmit_more())
 		airoha_qdma_rmw(qdma, REG_TX_CPU_IDX(qid),
 				TX_RING_CPU_IDX_MASK,
-				FIELD_PREP(TX_RING_CPU_IDX_MASK, q->head));
+				FIELD_PREP(TX_RING_CPU_IDX_MASK, index));
 
 	if (q->ndesc - q->queued < q->free_thr)
 		netif_tx_stop_queue(txq);
@@ -9991,10 +9987,13 @@ int airoha_eth_transmit_packet(struct sk
 	return NETDEV_TX_OK;
 
 error_unmap:
-	for (i--; i >= 0; i--) {
-		index = (q->head + i) % q->ndesc;
-		dma_unmap_single(dev->dev.parent, q->entry[index].dma_addr,
-				 q->entry[index].dma_len, DMA_TO_DEVICE);
+	while (!list_empty(&tx_list)) {
+		e = list_first_entry(&tx_list, struct airoha_queue_entry,
+				     list);
+		dma_unmap_single(dev->dev.parent, e->dma_addr, e->dma_len,
+				 DMA_TO_DEVICE);
+		e->dma_addr = 0;
+		list_move_tail(&e->list, &q->tx_list);
 	}
 
 	spin_unlock_irq(&q->lock);
@@ -10035,8 +10034,8 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 	struct skb_shared_info *sinfo = skb_shinfo(skb);
 	
 	struct airoha_gdm_port *port = glb_eth->ports[1];
-	
-	struct net_device *dev = port->dev;
+
+	struct net_device *dev = port->devs[0]->dev;
 	struct airoha_foe_entry *hwe;
 	u32  len = skb_headlen(skb);
 	
@@ -10050,8 +10049,8 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 	u16 index;
 	u32 msg2 = 0;
 
-	//qid = pinfo->channel;
-	
+	skb->dev = dev;
+
 	msg0 |= FIELD_PREP(QDMA_ETH_TXMSG_TCO_MASK, 1);
 	if(isPriorityPkt(skb)){
 		msg0 |= FIELD_PREP(QDMA_ETH_TXMSG_QUEUE_MASK, 7);
@@ -10094,19 +10093,24 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 	}
 
 	txq = netdev_get_tx_queue(dev, qid);
-	if (q->queued + nr_frags > q->ndesc) {
-		/* not enough space in the queue */
+	if (q->queued + nr_frags >= q->ndesc) {
 		netif_tx_stop_queue(txq);
 		spin_unlock_irq(&q->lock);
 		return -1;
 	}
 
+	{
+	struct airoha_queue_entry *e;
+	LIST_HEAD(tx_list);
+
 	data = skb->data;
-	index = q->head;
+
+	e = list_first_entry(&q->tx_list, struct airoha_queue_entry,
+			     list);
+	index = e - q->entry;
 
 	for (i = 0; i < nr_frags; i++) {
 		struct airoha_qdma_desc *desc = &q->desc[index];
-		struct airoha_queue_entry *e = &q->entry[index];
 		skb_frag_t *frag = &sinfo->frags[i];
 		dma_addr_t addr;
 		u32 val;
@@ -10116,7 +10120,14 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 		if (unlikely(dma_mapping_error(dev->dev.parent, addr)))
 			goto error_unmap;
 
-		index = (index + 1) % q->ndesc;
+		list_move_tail(&e->list, &tx_list);
+		e->skb = i ? NULL : skb;
+		e->dma_addr = addr;
+		e->dma_len = len;
+
+		e = list_first_entry(&q->tx_list, struct airoha_queue_entry,
+				     list);
+		index = e - q->entry;
 
 		val = FIELD_PREP(QDMA_DESC_LEN_MASK, len);
 		if (i < nr_frags - 1)
@@ -10129,15 +10140,9 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 		WRITE_ONCE(desc->msg1, cpu_to_le32(msg1));
 		WRITE_ONCE(desc->msg2, cpu_to_le32(msg2));
 
-		e->skb = i ? NULL : skb;
-		e->dma_addr = addr;
-		e->dma_len = len;
-
 		data = skb_frag_address(frag);
 		len = skb_frag_size(frag);
 	}
-
-	q->head = index;
 	q->queued += i;
 
 	skb_tx_timestamp(skb);
@@ -10146,7 +10151,7 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 	if (netif_xmit_stopped(txq) || !netdev_xmit_more())
 		airoha_qdma_rmw(qdma, REG_TX_CPU_IDX(qid),
 				TX_RING_CPU_IDX_MASK,
-				FIELD_PREP(TX_RING_CPU_IDX_MASK, q->head));
+				FIELD_PREP(TX_RING_CPU_IDX_MASK, index));
 
 	if (q->ndesc - q->queued < q->free_thr)
 		netif_tx_stop_queue(txq);
@@ -10156,10 +10161,14 @@ int qdma_wan_tx(struct sk_buff *skb, u32
 	return 0;
 
 error_unmap:
-	for (i--; i >= 0; i--) {
-		index = (q->head + i) % q->ndesc;
-		dma_unmap_single(dev->dev.parent, q->entry[index].dma_addr,
-				 q->entry[index].dma_len, DMA_TO_DEVICE);
+	while (!list_empty(&tx_list)) {
+		e = list_first_entry(&tx_list, struct airoha_queue_entry,
+				     list);
+		dma_unmap_single(dev->dev.parent, e->dma_addr, e->dma_len,
+				 DMA_TO_DEVICE);
+		e->dma_addr = 0;
+		list_move_tail(&e->list, &q->tx_list);
+	}
 	}
 
 	spin_unlock_irq(&q->lock);
@@ -10219,9 +10228,9 @@ void airoha_ppe_foe_flow_update_wifi_npu
 	
 	type = FIELD_GET(AIROHA_FOE_IB1_BIND_PACKET_TYPE, hwe->ib1);
 	AIROHA_LOG(AIROHA_DEBUG_LEVEL_INFO, "Packet type:%d\n", type);
-	index = airoha_ppe_foe_get_entry_hash(hwe);
-	
-	if(airoha_ppe_is_wifi2G_dev(skb->dev)){		
+	index = airoha_ppe_foe_get_entry_hash(ppe, hwe);
+
+	if(airoha_ppe_is_wifi2G_dev(skb->dev)){
 		pinfo->nbq = 0;
 	}
 	else{		
@@ -10863,8 +10872,8 @@ int arht_multicast_hwnat_state_handler_l
 
 	airoha_ppe_foe_commit_entry(ppe, hwe, foe_index);
 
-	index = airoha_ppe_foe_get_entry_hash(hwe);
-								
+	index = airoha_ppe_foe_get_entry_hash(ppe, hwe);
+
 	e = kzalloc(sizeof(*e), GFP_KERNEL);
 	if (!e)
 		return -ENOMEM;
@@ -11563,9 +11572,9 @@ int speedtest_tx_offload(struct sk_buff
 }
 
 int airoha_eth_fast_tx(struct sk_buff *skb, int channel){
-	struct net_device *dev = glb_eth->ports[0]->dev;
-	struct airoha_gdm_port *port = netdev_priv(dev);
-	struct airoha_qdma *qdma = port->qdma;
+	struct airoha_gdm_port *port = glb_eth->ports[0];
+	struct net_device *dev = port->devs[0]->dev;
+	struct airoha_qdma *qdma = port->devs[0]->qdma;
 	u32 nr_frags, msg0, msg1, len;
 	struct netdev_queue *txq;
 	struct airoha_queue *q;
@@ -11623,67 +11632,75 @@ int airoha_eth_fast_tx(struct sk_buff *s
 	txq = netdev_get_tx_queue(dev, qid);
 	nr_frags = 1 + skb_shinfo(skb)->nr_frags;
 
-	if (q->queued + nr_frags > q->ndesc) {
-		/* not enough space in the queue */
+	if (q->queued + nr_frags >= q->ndesc) {
 		spin_unlock_irq(&q->lock);
 		goto error;
 	}
 
+	{
+	struct airoha_queue_entry *e;
+	LIST_HEAD(tx_list);
+
 	len = skb_headlen(skb);
 	data = skb->data;
-	index = q->head;
+
+	e = list_first_entry(&q->tx_list, struct airoha_queue_entry,
+			     list);
+	index = e - q->entry;
 
 	for (i = 0; i < nr_frags; i++) {
 		struct airoha_qdma_desc *desc = &q->desc[index];
-		struct airoha_queue_entry *e = &q->entry[index];
 		skb_frag_t *frag = &skb_shinfo(skb)->frags[i];
 		dma_addr_t addr;
 		u32 val;
+
 		addr = dma_map_single(dev->dev.parent, data, len,
-					  DMA_TO_DEVICE);
+				      DMA_TO_DEVICE);
 		if (unlikely(dma_mapping_error(dev->dev.parent, addr)))
 			goto error_unmap;
 
-		index = (index + 1) % q->ndesc;
+		list_move_tail(&e->list, &tx_list);
+		e->skb = i ? NULL : skb;
+		e->dma_addr = addr;
+		e->dma_len = len;
+
+		e = list_first_entry(&q->tx_list, struct airoha_queue_entry,
+				     list);
+		index = e - q->entry;
 
 		val = FIELD_PREP(QDMA_DESC_LEN_MASK, len);
-		if (i < nr_frags - 1){
+		if (i < nr_frags - 1)
 			val |= FIELD_PREP(QDMA_DESC_MORE_MASK, 1);
-		}
 		WRITE_ONCE(desc->ctrl, cpu_to_le32(val));
 		WRITE_ONCE(desc->addr, cpu_to_le32(addr));
-
 		val = FIELD_PREP(QDMA_DESC_NEXT_ID_MASK, index);
 		WRITE_ONCE(desc->data, cpu_to_le32(val));
 		WRITE_ONCE(desc->msg0, cpu_to_le32(msg0));
 		WRITE_ONCE(desc->msg1, cpu_to_le32(msg1));
 
-		e->skb = i ? NULL : skb;
-		e->dma_addr = addr;
-		e->dma_len = len;
-
 		data = skb_frag_address(frag);
 		len = skb_frag_size(frag);
 	}
-	
-
-	q->head = index;
 	q->queued += i;
 
 	netdev_tx_sent_queue(txq, skb->len);
 	airoha_qdma_rmw(qdma, REG_TX_CPU_IDX(qid),
 			TX_RING_CPU_IDX_MASK,
-			FIELD_PREP(TX_RING_CPU_IDX_MASK, q->head));
+			FIELD_PREP(TX_RING_CPU_IDX_MASK, index));
 
 	spin_unlock_irq(&q->lock);
 
 	return NETDEV_TX_OK;
 
 error_unmap:
-	for (i--; i >= 0; i--) {
-		index = (q->head + i) % q->ndesc;
-		dma_unmap_single(dev->dev.parent, q->entry[index].dma_addr,
-				 q->entry[index].dma_len, DMA_TO_DEVICE);
+	while (!list_empty(&tx_list)) {
+		e = list_first_entry(&tx_list, struct airoha_queue_entry,
+				     list);
+		dma_unmap_single(dev->dev.parent, e->dma_addr, e->dma_len,
+				 DMA_TO_DEVICE);
+		e->dma_addr = 0;
+		list_move_tail(&e->list, &q->tx_list);
+	}
 	}
 
 	spin_unlock_irq(&q->lock);
@@ -11996,13 +12013,13 @@ int airoha_qdma_set_tx_rate_limit(struct
 	int i, err;
 
 	for (i = 0; i <= TRTCM_PEAK_MODE; i++) {
-		err = airoha_qdma_set_trtcm_config(port->qdma, channel,
+		err = airoha_qdma_set_trtcm_config(port->devs[0]->qdma, channel,
 						   REG_EGRESS_TRTCM_CFG, i,
 						   !!rate, TRTCM_METER_MODE);
 		if (err)
 			return err;
 
-		err = airoha_qdma_set_trtcm_token_bucket(port->qdma, channel,
+		err = airoha_qdma_set_trtcm_token_bucket(port->devs[0]->qdma, channel,
 							 REG_EGRESS_TRTCM_CFG,
 							 i, rate, bucket_size);
 		if (err)
@@ -12248,9 +12265,9 @@ static void airoha_qdma_start_channel(st
 {
 	
 	int  i = 0;
-	if(port->qdma){
+	if(port->devs[0] && port->devs[0]->qdma){
 		for (i = 0; i < AIROHA_NUM_QOS_QUEUES; i++)
-			airoha_qdma_clear(port->qdma, REG_QUEUE_CLOSE_CFG(channel),
+			airoha_qdma_clear(port->devs[0]->qdma, REG_QUEUE_CLOSE_CFG(channel),
 					  TXQ_DISABLE_CHAN_QUEUE_MASK(channel, i));	
 	}
 }
@@ -12258,9 +12275,9 @@ static void airoha_qdma_start_channel(st
 static void airoha_qdma_stop_channel(struct airoha_gdm_port *port,uint channel)
 {
 	int  i = 0;
-	if(port->qdma){
+	if(port->devs[0] && port->devs[0]->qdma){
 		for (i = 0; i < AIROHA_NUM_QOS_QUEUES; i++)
-			airoha_qdma_set(port->qdma, REG_QUEUE_CLOSE_CFG(channel),
+			airoha_qdma_set(port->devs[0]->qdma, REG_QUEUE_CLOSE_CFG(channel),
 					  TXQ_DISABLE_CHAN_QUEUE_MASK(channel, i));
 	}
 }
@@ -12452,8 +12469,8 @@ void airoha_eth_monitor_gsw_process(stru
 
 void airoha_eth_monitor_serdes_process(struct airoha_gdm_port *port)
 {
-	struct net_device *eth_dev = port->dev;
-	/*Detect Link Status*/ 
+	struct net_device *eth_dev = port->devs[0] ? port->devs[0]->dev : NULL;
+	/*Detect Link Status*/
 	/*Link down to up*/
 	if (!eth_dev){
 		printk("Error ! %s: Invaild eth dev port->id:%d\n",__func__,port->id);
@@ -12462,7 +12479,7 @@ void airoha_eth_monitor_serdes_process(s
 	
 	if (!eth_lastlinks[ARHT_ETH_PORT_2] && (netif_running(eth_dev)&& netif_carrier_ok(eth_dev))){
 		if (glb_eth){
-			if (port && port->qdma){
+			if (port && port->devs[0] && port->devs[0]->qdma){
 				airoha_eth_monitor_link_up_protection(port, eth_dev, HSGMII_LAN_ETH_CHNL);
 			}
 		}
@@ -12471,7 +12488,7 @@ void airoha_eth_monitor_serdes_process(s
 				/* Link up to down*/
 	}else if (eth_lastlinks[ARHT_ETH_PORT_2] && (!netif_running(eth_dev) || !netif_carrier_ok(eth_dev))){	
 		if (glb_eth){
-			if (port && port->qdma){
+			if (port && port->devs[0] && port->devs[0]->qdma){
 				/*Stop QDMA channel & MAC when Link Down*/
 				airoha_eth_monitor_link_down_protection(port, eth_dev, HSGMII_LAN_ETH_CHNL);
 			}
@@ -12494,8 +12511,8 @@ void airoha_eth_monitor(struct work_stru
 		struct airoha_gdm_port *port = glb_eth->ports[p];
 		struct net_device *eth_dev = NULL;
 		
-		eth_dev = port->dev;
-		
+		eth_dev = port->devs[0] ? port->devs[0]->dev : NULL;
+
 		if(netdev_uses_dsa(eth_dev)){
 			/*Detect eth0 - lan1~4*/
 			airoha_eth_monitor_gsw_process(port);
--- a/drivers/net/ethernet/airoha/airoha_eth.h
+++ b/drivers/net/ethernet/airoha/airoha_eth.h
@@ -165,13 +165,6 @@ enum {
 	CRSN_25 = 0x19,
 };
 
-enum airoha_gdm_index {
-	AIROHA_GDM1_IDX = 1,
-	AIROHA_GDM2_IDX = 2,
-	AIROHA_GDM3_IDX = 3,
-	AIROHA_GDM4_IDX = 4,
-};
-
 enum {
 	FE_PSE_PORT_CDM1,
 	FE_PSE_PORT_GDM1,
@@ -953,7 +946,12 @@ int airoha_get_lan_id(struct net_device
 void airoha_flow_table_entries_lan(struct rhashtable *flow_table, struct net_device *dev);
 void airoha_fe_core_reset(struct airoha_eth *eth);
 int airoha_receive_hook(struct sk_buff *skb, struct airoha_qdma_desc *desc, int p, int len, u32 sptag);
-
+u32 airoha_ppe_foe_get_entry_hash(struct airoha_ppe *ppe, struct airoha_foe_entry *hwe);
+void airoha_ppe_clear_sram_table_flow(struct airoha_ppe *ppe,uint entry_idx);
+void airoha_ppe_clear_sram_table_all(struct airoha_ppe *ppe);
+void airoha_ppe_clean_sram_table(void);
+void airoha_ppe_delete_entry(struct airoha_ppe *ppe, struct airoha_foe_entry *hwe, uint entry_idx);
+bool airoha_ppe_foe_compare_entry(struct airoha_flow_table_entry *e, struct airoha_foe_entry *hwe);
 
 /*QDMA Multicast*/
 #define MULTICAST_SPTAG_KEEP_HI_EN_SHIFT			(31)
--- a/drivers/net/ethernet/airoha/arht_dp_api_debugfs.c
+++ b/drivers/net/ethernet/airoha/arht_dp_api_debugfs.c
@@ -414,7 +414,7 @@ static int airoha_ppe_debugfs_foe_flow_s
 		[FLOW_TYPE_L2_SUBFLOW] = "FLOW_TYPE_L2_SUBFLOW",
 	};
 
-	for (i = 0; i < PPE_NUM_ENTRIES; i++) 
+	for (i = 0; i < airoha_ppe_get_total_num_entries(ppe); i++) 
 	{
 		hlist_for_each_entry_safe(e, n, &ppe->foe_flow[i], list) 
 		{
--- a/drivers/net/ethernet/airoha/Makefile
+++ b/drivers/net/ethernet/airoha/Makefile
@@ -6,6 +6,6 @@
 obj-$(CONFIG_NET_AIROHA) += airoha-eth.o
 obj-$(CONFIG_NET_AIROHA) += arht_dp_api.o
 obj-$(CONFIG_NET_AIROHA) += arht_dp_api_debugfs.o
-airoha-eth-y := airoha_eth.o airoha_ppe.o
+airoha-eth-y := airoha_eth.o airoha_ppe.o eth_sal.o en7581/soc_en7581.o
 airoha-eth-$(CONFIG_DEBUG_FS) += airoha_ppe_debugfs.o
 obj-$(CONFIG_NET_AIROHA_NPU) += airoha_npu.o
--- a/drivers/net/ethernet/airoha/en7581/soc_en7581.c
+++ b/drivers/net/ethernet/airoha/en7581/soc_en7581.c
@@ -59,7 +59,7 @@ static void en7581_get_gdm_loopback_info
 	*pse_port = (portid == AIROHA_GDM4_IDX) ? FE_PSE_PORT_GDM3 : FE_PSE_PORT_GDM4;
 }
 
-void en7581_set_sp_dft_cport(struct airoha_eth *eth, int portid)
+static void en7581_set_sp_dft_cport(struct airoha_eth *eth, int portid)
 {
     //unsigned char srcport_idx = eth->chip->dma_srcport[portid];
 	
@@ -68,23 +68,24 @@ void en7581_set_sp_dft_cport(struct airo
 		/* FIXME: handle XSI_PCE1_PORT */
 		airoha_fe_rmw(eth, REG_FE_WAN_PORT,
 			      WAN1_EN_MASK | WAN1_MASK | WAN0_MASK,
-			      FIELD_PREP(WAN0_MASK, HSGMII_LAN_PCIE0_SRCPORT));
+			      FIELD_PREP(WAN0_MASK, HSGMII_LAN_7581_PCIE0_SRCPORT));
 		airoha_fe_rmw(eth,
-			      REG_SP_DFT_CPORT(HSGMII_LAN_PCIE0_SRCPORT >> 3),
-			      SP_CPORT_PCIE0_MASK,
-			      FIELD_PREP(SP_CPORT_PCIE0_MASK,
+			      REG_SP_DFT_CPORT(HSGMII_LAN_7581_PCIE0_SRCPORT >> 3),
+			      SP_CPORT_MASK(HSGMII_LAN_7581_PCIE0_SRCPORT & 0x7),
+			      FIELD_PREP(SP_CPORT_MASK(HSGMII_LAN_7581_PCIE0_SRCPORT & 0x7),
 					 FE_PSE_PORT_CDM2));
 		break;
 	case AIROHA_GDM4_IDX:
 		airoha_fe_rmw(eth, REG_SRC_PORT_FC_MAP6,
-			      FC_ID_OF_SRC_PORT24_MASK,
-			      FIELD_PREP(FC_ID_OF_SRC_PORT24_MASK, 2));
+			      FC_ID_OF_SRC_PORT_MASK(0),
+			      FIELD_PREP(FC_ID_OF_SRC_PORT_MASK(0), 2));
 		airoha_fe_rmw(eth, REG_FE_WAN_PORT,
 			      WAN1_EN_MASK | WAN1_MASK | WAN0_MASK,
-			      FIELD_PREP(WAN0_MASK, HSGMII_LAN_ETH_SRCPORT));
-		airoha_fe_rmw(eth, REG_SP_DFT_CPORT(HSGMII_LAN_ETH_SRCPORT >> 3),
-			      SP_CPORT_ETH_MASK,
-			      FIELD_PREP(SP_CPORT_ETH_MASK, FE_PSE_PORT_CDM2));
+			      FIELD_PREP(WAN0_MASK, HSGMII_LAN_7581_ETH_SRCPORT));
+		airoha_fe_rmw(eth, REG_SP_DFT_CPORT(HSGMII_LAN_7581_ETH_SRCPORT >> 3),
+			      SP_CPORT_MASK(HSGMII_LAN_7581_ETH_SRCPORT & 0x7),
+			      FIELD_PREP(SP_CPORT_MASK(HSGMII_LAN_7581_ETH_SRCPORT & 0x7),
+					 FE_PSE_PORT_CDM2));
 		break;
 	default:
 		break;
@@ -99,17 +100,17 @@ static int en7581_support_eth_monitor(in
 	return 1;
 }
 
-void an7581_qdma_regs_setting(struct airoha_qdma *qdma, struct airoha_eth *eth)
+static void an7581_qdma_regs_setting(struct airoha_qdma *qdma, struct airoha_eth *eth)
 {
 	return ;
 }
 
-void en7581_set_qbi_fttr_chn_disable(struct airoha_eth *eth)
+static void en7581_set_qbi_fttr_chn_disable(struct airoha_eth *eth)
 {
 	return;
 }
 
-void an7581_fe_regs_setting(struct airoha_eth *eth)
+static void an7581_fe_regs_setting(struct airoha_eth *eth)
 {
 	return;
 }
@@ -159,4 +160,4 @@ const struct airoha_eth_chip chip_en7581
 	.set_qdma_regs = an7581_qdma_regs_setting,
 	.set_qbi_fttr_chn = en7581_set_qbi_fttr_chn_disable,
 	.set_fe_regs = an7581_fe_regs_setting,
-};
\ No newline at end of file
+};
--- a/drivers/net/ethernet/airoha/airoha_eth.c
+++ b/drivers/net/ethernet/airoha/airoha_eth.c
@@ -3999,86 +3999,7 @@ static int airoha_en7581_get_dev_from_sp
 	return 0;
 }
 
-static const char * const an7583_xsi_rsts_names[] = {
-	"hsi0-mac",
-	"hsi1-mac",
-	"xfp-mac",
-};
-
-static int airoha_an7583_get_sport(struct airoha_gdm_port *port, int nbq)
-{
-	switch (port->id) {
-	case AIROHA_GDM3_IDX:
-		/* 7583 SoC supports eth serdes on GDM3 port */
-		if (!nbq)
-			return HSGMII_LAN_7583_ETH_SRCPORT;
-		break;
-	case AIROHA_GDM4_IDX:
-		/* 7583 SoC supports PCIe and USB serdes on GDM4 port */
-		if (!nbq)
-			return HSGMII_LAN_7583_PCIE_SRCPORT;
-		if (nbq == 1)
-			return HSGMII_LAN_7583_USB_SRCPORT;
-		break;
-	default:
-		break;
-	}
-
-	return -EINVAL;
-}
-
-static u32 airoha_an7583_get_vip_port(struct airoha_gdm_port *port, int nbq)
-{
-	switch (port->id) {
-	case AIROHA_GDM3_IDX:
-		if (!nbq)
-			return XSI_ETH_VIP_PORT_MASK;
-		break;
-	case AIROHA_GDM4_IDX:
-		if (!nbq)
-			return XSI_PCIE0_VIP_PORT_MASK;
-		if (nbq == 1)
-			return XSI_USB_VIP_PORT_MASK;
-		break;
-	default:
-		break;
-	}
-
-	return 0;
-}
-
-static int airoha_an7583_get_dev_from_sport(struct airoha_qdma_desc *desc,
-					    u16 *port, u16 *dev)
-{
-	u32 sport = FIELD_GET(QDMA_ETH_RXMSG_SPORT_MASK,
-			      le32_to_cpu(READ_ONCE(desc->msg1)));
-
-	*dev = 0;
-	switch (sport) {
-	case 0x10 ... 0x14:
-		*port = 0; /* GDM1 */
-		break;
-	case 0x2 ... 0x4:
-		*port = sport - 1;
-		break;
-	case HSGMII_LAN_7583_ETH_SRCPORT:
-		*port = 2; /* GDM3 */
-		break;
-	case HSGMII_LAN_7583_USB_SRCPORT:
-		*dev = 1;
-		fallthrough;
-	case HSGMII_LAN_7583_PCIE_SRCPORT:
-		*port = 3; /* GDM4 */
-		break;
-	default:
-		return -EINVAL;
-	}
-
-	return 0;
-}
-
 extern const struct airoha_eth_chip chip_en7581_info;
-extern const struct airoha_eth_chip chip_an7583_info;
 static const struct airoha_eth_soc_data en7581_soc_data = {
 	.version = 0x7581,
 	.xsi_rsts_names = en7581_xsi_rsts_names,
@@ -4092,22 +4013,8 @@ static const struct airoha_eth_soc_data
 	.chip = &chip_en7581_info,
 };
 
-static const struct airoha_eth_soc_data an7583_soc_data = {
-	.version = 0x7583,
-	.xsi_rsts_names = an7583_xsi_rsts_names,
-	.num_xsi_rsts = ARRAY_SIZE(an7583_xsi_rsts_names),
-	.num_ppe = 1,
-	.ops = {
-		.get_sport = airoha_an7583_get_sport,
-		.get_vip_port = airoha_an7583_get_vip_port,
-		.get_dev_from_sport = airoha_an7583_get_dev_from_sport,
-	},
-	.chip = &chip_an7583_info,
-};
-
 static const struct of_device_id of_airoha_match[] = {
 	{ .compatible = "airoha,en7581-eth", .data = &en7581_soc_data },
-	{ .compatible = "airoha,an7583-eth", .data = &an7583_soc_data },
 	{ /* sentinel */ }
 };
 MODULE_DEVICE_TABLE(of, of_airoha_match);
--- a/drivers/net/ethernet/airoha/airoha_ppe.c
+++ b/drivers/net/ethernet/airoha/airoha_ppe.c
@@ -16,6 +16,7 @@
 
 #include "airoha_regs.h"
 #include "airoha_eth.h"
+#include "eth_sal.h"
 
 extern int airoha_set_ppe_mac(struct airoha_foe_entry *foe_entry, struct net_device *dev, char* src_mac, char*dst_mac, u16 pppid);
 extern int (*ra_sw_nat_hook_clean_table) (void);
@@ -446,7 +447,7 @@ static int airoha_ppe_foe_entry_prepare(
 
 			smac_id = port->id;
 
-			id = port->qdma - &eth->qdma[0];
+			id = dev->qdma - &eth->qdma[0];
 			if (dsa_port >= 0) {
 				if (eth->qdma_init.lan_fastpath == 1) {
 					val |= AIROHA_FOE_IB2_FAST_PATH;
@@ -500,7 +501,7 @@ static int airoha_ppe_foe_entry_prepare(
 		l2->etype = ETH_P_IP;
 	}
 
-	airoha_set_ppe_mac(hwe, dev, data->eth.h_source, data->eth.h_dest, data->pppoe.sid);
+	airoha_set_ppe_mac(hwe, netdev, data->eth.h_source, data->eth.h_dest, data->pppoe.sid);
 
 	if (data->vlan.num) {
 		l2->vlan1 = data->vlan.hdr[0].id;
